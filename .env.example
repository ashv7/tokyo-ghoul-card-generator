# ========================================
# TOKYO GHOUL CARD GENERATOR - ENVIRONMENT VARIABLES
# ========================================

# JWT Secret Key (IMPORTANT: Changez ceci en production!)
# Générez un secret fort avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=tokyo-ghoul-secret-key-change-in-production

# MongoDB Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=

# API URL (pour le frontend)
# En développement: http://localhost:3000
# En production: https://votre-site.netlify.app/.netlify/functions
VITE_API_URL=http://localhost:3000

# Port du serveur (développement uniquement)
PORT=3000

# Node Environment
NODE_ENV=development
